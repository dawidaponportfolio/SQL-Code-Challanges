SELECT ROUND(long_w, 4)
FROM station
WHERE (ROUND(LAT_N, 4)) < 137.2345
ORDER BY LAT_N DESC
LIMIT 1
;


-- or

WITH CTE AS
(
SELECT LONG_W, LAT_N 
FROM STATION
WHERE LAT_N < 137.2345
ORDER BY LAT_N DESC
LIMIT 1
)
SELECT ROUND(LONG_W, 4)
FROM CTE
;